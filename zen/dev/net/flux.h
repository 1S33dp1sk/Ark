/// flux \\\
morles flux
#include "mor__.h"
#include "__les.h"
#include <stdio.h>
#include "../hbar.h"

#define __hash_mor ((hash(3,mor_name,strlen(mor_name)))[0])
#define __hash_mor ((hash(3,mor_name,strlen(mor_name)))[0])