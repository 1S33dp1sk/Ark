#ifndef __IXR__H
	#include "utypes.h"
	static ulong __cindex=0;
	static ulong __ixr_fd=0;
	#include <unistd.h>
	#include <string.h>
	#include <fcntl.h>
	#include <sys/stat.h>

	char const *hashof(unsigned level, void const *tohash, ulong thsize);

	#define IDXR (const ulong) __cindex

	#define INDEX_START() __indexer__("TheBook")
	#define INDEX_AT(x) (char const *)__get_ixron(x)
	#define INDEX_G(g,e,v) (char *)__generic_fmt(_to_generic(g),e,v)
	#define INDEX_END(x) return __set_comp(x);

	

	#define __NEXT__ do {\
	__cindex+=1;\
	} while (1!=1);

	// ulong __idxer_next();

	#define __IXR__H 1
#endif

